<%- include('ek/header', {bot, user, path}) %>
<div class="container">
  <div align="center" class="jumbotron">

       <img src="https://cdn.discordapp.com/avatars/<%=user.id%>/<%=user.avatar%>.gif?size=512" class="rounded-circle panel">
       <h3 style="color:#fff"><%=user.username%>#<%=user.discriminator%></h3>
       <h5 style="color:#fff">Profile Info</h5>

       <% if (bot.profile[user.id]) { %>
        <h6 style="color:#fff">Name: <%= bot.profile[user.id].name || "Unknown" %></h6><br>
        <h6 style="color:#fff">Age: <%= bot.profile[user.id].age || "Unknown" %></h6><br>
        <h6 style="color:#fff">Biography: <%= bot.profile[user.id].bio %></h6>
        <h6 style="color:#ffff">Favorite coding language: <%= bot.profile[user.id].favlib || "No" %></h6><br>
        <h6 style="color:#fff">Coding languages it knows: <%= bot.profile[user.id].libs || "No" %></h6>
       <% } else { %>
       <% } %>
    
     <a href="/user/<%=user.id%>/profile/adjust" class="btn btn-default" style="background-color:#333; color:#fff">Edit Profile</a>

  </div>

  <h3 style="color:#222"><%=user.tag%> Your Discord4Bots</h3>
<br>
  <% if (bot.ayar.has('bots')) { %>
    <% if (bot.ayar.has(`kbots.${user.id}`)) { %>   

<%for(var i = 0; i < Object.keys(bot.ayar.fetch('bots')).length; i++) {%>
<% var idd = Object.keys(bot.ayar.fetch('bots'))[i] %>
<%  if (bot.ayar.fetch(`bots.${id}.ownerid`) === user.id) { %>
<div class="col-12 col-sm-6 col-md-4">
     <div class="card card-lg botcard" style="background-color:#fff; width:16rem;">
  <div align="center" class="card-img">
      <a href="/bot/<%=id%>"><img src="<%=bot.ayar.fetch(`bots.${id}.avatar`)%>" class="card-img-top" alt="Bot Avatar"></a>
    <% if (bot.ayar.has(`bots.${id}.certificate`) === true) { %>
    <br><br><a style="color:darkorange"><img src="https://cdn.discordapp.com/emojis/522886304615366676.png" width="30px" height="30px"> Certificate Bot</a><br>
    <% } %>

   <%  if(bot.ayar.fetch(`bots.${id}.status`) === 'Pending' || !bot.ayar.has(`bots.${id}.status`)) { %>
    <div class="badge badge-warning">Pending</div>
    <% } %>

    <%  if(bot.ayar.fetch(`bots.${id}.status`) === 'Removed') { %>
    <div class="badge badge-danger">Removed</div>
    <% } %>

    <%  if(bot.ayar.fetch(`bots.${id}.status`) === 'Approved') { %>
    <div class="badge badge-success">Approved</div>
    <% } %>

    <div class="badge badge-primary"><strong style='color: white;'>Votes: </strong><span style='color:whitesmoke;text-transform: none;'><%=bot.ayar.fetch(`bots.${id}.vote`)||0%></span></div>
                 <div style="background-color:grey; color:#fff" class="badge badge-default"><%=bot.ayar.fetch(`bots.${id}.library`)%></div>
                         </div>
                          <div align="center" style="background-color:#fff" class="card-block">
                             <div class="card-title">
                               <h4><a style="color:#222"><%=bot.useful.seg(bot.ayar.fetch(`bots.${id}.name`), 5)%></a></h4>
                             <h6 style="font-size:17px; color:#222"><%=bot.ayar.fetch(`bots.${id}.owner`)%></h6></div>         
                     
                           <p class="card-text" style="color:#222"><%=bot.ayar.fetch(`bots.${id}.ShortDesc`)%></p>
                            <hr>
                            <h6 align="center"><a href="/bot/<%=id%>" style="font-size:20px; color:#222; background-color:#fff">view</a> <b style="color:grey">|</b> 
                              <a href="https://discordapp.com/oauth2/authorize?client_id=<%=id%>&scope=bot&permissions=8" style="font-size:20px; color:#222; background-color:#fff">Invite</a></h6>
                            </div>
                    </div><br>
</div>
<% } %>
<% } %>
</div>
   
   <% } else if (!bot.ayar.has(`kbots.${user.id}`)) {%>
        <h5 style="color:#222">
          Sorry, you didn't add your bot on Discord4Bots!
   </h5>
   <% } %>
   <% } else if (!bot.ayar.has('bots')) {%>
        <h5 style="color:#222">
          Sorry, you didn't add your bot on Discord4Bots!
   </h5>
   <% } %>

</div></div>
<% include ./ek/footer %>
